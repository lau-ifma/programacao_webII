<div class="container-sm mt-2">
    <nav class="navbar bg-body-tertiary rounded-top-4 rounded-bottom-0">
        <div class="container-fluid">
            <h3 class="navbar-brand">Usuarios</h3>

            <ul class="nav">
                <li class="nav-item">
                    <a class="btn btn-sm btn-primary mx-2" href="/usuario/cadastro"><i class="fa-solid fa-plus"></i> Novo</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-sm btn-secondary" href="/" ><i class="fa-solid fa-angle-left"></i> Voltar</a>
                </li>
            </ul>
        </div>
    </nav>

    <table class="table table-striped">
        <tr>
            <th class="col-1">ID</th>
            <th class="col-1">USERNAME</th>
            <th class="col-5">SENHA</th>
            <th class="col-2">CATEGORIA</th>
            <th class="col-2">STATUS</th>
            <th class="col-2">PESSOA_ID</th>

        </tr>
        {{#each usuarios}}
            <tr>
                <td>{{id}}</td>
                <td>{{username}}</td>
                <td>{{senha}}</td>
                <td>{{categoria}}</td>
                <td>{{status}}</td>
                <td>{{pessoa_id}}</td>
            </tr>

        {{/each}}

    </table>

</div>
